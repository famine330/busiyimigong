import basic
import utils
import time


if __name__ == "__main__":  
    handle = basic.get_handle()
    now_floor = utils.floor(handle)
    while True:
        now_floor.SL_basic()
        time.sleep(120)
        